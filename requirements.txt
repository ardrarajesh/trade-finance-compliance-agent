# Core (Module 0-1). We add more per module so you understand every dependency.
# Pin loosely for a portfolio project; we can freeze exact versions at the end.

# Data models & validation (used everywhere for structured output)
pydantic>=2.6

# Synthetic document generation (Module 1)
reportlab>=4.0        # generate realistic PDF documents
faker>=24.0           # realistic fake names, dates, addresses

# Ingestion (Module 2)
pdfplumber>=0.11      # extract embedded text (and layout) from PDFs

# LLM layer (Module 3)
ollama>=0.3           # thin client for a local Ollama server

# Orchestration (Module 6)
langgraph>=0.2        # state-machine orchestration of the pipeline

# API + UI (Module 7)
fastapi>=0.110        # REST service
uvicorn>=0.29         # ASGI server to run FastAPI
python-multipart>=0.0.9  # file uploads for FastAPI
streamlit>=1.33       # upload UI (client of the API)
requests>=2.31        # UI -> API HTTP calls

# Config & environment
python-dotenv>=1.0

# Dev / quality
pytest>=8.0
